# Shell scripts MUST be LF or they break on macOS/Linux (bad interpreter, \r everywhere).
*.sh    text eol=lf
# Windows-only scripts stay CRLF.
*.bat   text eol=crlf
*.ps1   text eol=crlf
# Docs — normalize to LF, works everywhere.
*.md    text eol=lf
LICENSE text eol=lf
