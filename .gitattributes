# Shell scripts MUST be LF on GitHub, else macOS/Linux users get
# `bad interpreter: /usr/bin/env bash^M` when running via curl | bash.
# This is the only line-ending constraint we actually care about.
*.sh text eol=lf
